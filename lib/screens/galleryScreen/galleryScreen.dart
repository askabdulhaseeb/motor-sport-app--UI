import 'package:flutter/material.dart';

class GalleryScreen extends StatelessWidget {
  static final routeName = '/GalleryScreen';
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Gallery Screen'),
    );
  }
}
