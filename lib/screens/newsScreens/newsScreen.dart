import 'package:flutter/material.dart';

class NewsScreen extends StatelessWidget {
  static final routeName = '/NewsScreen';
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('NewsScreen'),
    );
  }
}
