import 'package:flutter/material.dart';

class MediaScreen extends StatelessWidget {
  static final routeName = '/MediaScreen';
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Media Screen'),
    );
  }
}
