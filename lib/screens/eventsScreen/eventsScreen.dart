import 'package:flutter/material.dart';

class EventsScreen extends StatelessWidget {
  static final routeName = '/EventsScreen';
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Events Screen'),
    );
  }
}
